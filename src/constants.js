import path from "path";

export const PUBLIC_DIR_PATH = path.join(process.cwd(), "public");
export const AVATARS_DIR_PATH = path.join(PUBLIC_DIR_PATH, "avatars");

export const TEMP_DIR_PATH = path.join(process.cwd(), "temp");
